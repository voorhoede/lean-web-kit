/**
 * Moved scrollBehavior to ~/app/router.scrollBehavior.js
 *
 * @see https://nuxtjs.org/api/configuration-router
 * Starting from v2.9.0, you can use a file to overwrite the router scrollBehavior,
 * this file should be placed in ~/app/router.scrollBehavior.js (note: filename is case-sensitive if running on Windows).
 */
